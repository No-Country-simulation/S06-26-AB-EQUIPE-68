package com.bitsystem.bitapp.controller;

/**
 * ============================================================================
 * WebController — DESATIVADO
 * ============================================================================
 *
 * Este controller foi esvaziado pois o BiT App migrou para arquitetura
 * desacoplada: o backend é agora 100% REST API e o frontend é servido
 * como serviço estático separado.
 *
 * As rotas que existiam aqui foram migradas para o ApiController:
 * - POST /onboarding  →  POST /api/usuarios
 *
 * As rotas de renderização Thymeleaf (GET /, GET /dashboard) foram
 * removidas junto com a dependência do Thymeleaf no pom.xml.
 *
 * @author BiT System
 * @version 2.0.0
 */
public class WebController {
    // Vazio — mantido apenas para histórico. Pode ser deletado com segurança.
}
